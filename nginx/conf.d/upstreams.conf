# ===========================================================
# 核心服务上游定义
# ===========================================================

# 后端 API 服务
upstream deer-flow-backend {
    server backend:8000;
}

# 前端 Next.js 服务
upstream deer-flow-frontend {
    server frontend:3000;
}
